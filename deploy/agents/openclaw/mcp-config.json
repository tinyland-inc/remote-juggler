{
  "mcpServers": {
    "rj-gateway": {
      "url": "https://rj-gateway.tail1234.ts.net/mcp",
      "transport": "http",
      "description": "RemoteJuggler MCP gateway with composite credential resolution"
    },
    "remote-juggler": {
      "command": "remote-juggler",
      "args": [
        "--mode=mcp"
      ],
      "description": "Direct RemoteJuggler CLI (fallback if gateway unavailable)"
    }
  }
}
